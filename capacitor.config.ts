import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aflac.awesome.app',
  appName: 'Best App In The Entire World',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
